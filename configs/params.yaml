project:
  name: saas_churn_mlops

data:
  observation_window_days: 90
  prediction_window_days: 30

features:
  version: v2

model:
  type: random_forest
  n_estimators: 300
  max_depth: 5
  min_samples_leaf: 5
  class_weight: balanced
  random_state: 42
 
training:
  test_size: 0.2
